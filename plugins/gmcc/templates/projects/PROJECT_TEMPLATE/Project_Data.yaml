# Project_Data.yaml — per-project identity and metadata.
#
# Copied by detect_repo.sh on first detection of a new project. The literal
# string PROJECT_TEMPLATE_NAME is rewritten to the actual project name at copy time.
#
# Schema is intentionally minimal in v6.0.0 — fuller schema lands in a follow-up.

version: 1
name: PROJECT_TEMPLATE_NAME
created_at: PROJECT_TEMPLATE_CREATED_AT
description: ""
# Future fields (deferred): owners, primary_language, kbite_associations, notes.
